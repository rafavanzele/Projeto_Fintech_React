import './Button.css'

const Button = () => {
    return (
        <div className="btn-container">
            <button className="btn-editar">
                Editar
            </button>

        </div>
    )
}

export default Button
